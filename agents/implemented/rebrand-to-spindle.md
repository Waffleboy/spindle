# Rebrand to Spindle

## Task
Rename the app from "Taxonomy Discovery Engine" to "Spindle" and integrate the custom logo from `logo.html`.

## Changes

### Frontend
- **`index.html`** — Updated `<title>` to "Spindle", added Google Fonts preconnect for Instrument Serif
- **`src/App.tsx`** — Replaced the indigo "T" icon + "Taxonomy Discovery Engine" header with the inline Spindle SVG logo (threads converging to diamond core) + "Spindle" wordmark in Instrument Serif + "Structure from chaos" tagline
- **`public/favicon.svg`** — Replaced the old purple lightning bolt favicon with the Spindle logo mark (thread convergence SVG)
- **`dist/favicon.svg`** — Updated to match
